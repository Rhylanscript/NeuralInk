# NeuralInk

A handwriting digit recognizer built entirely from scratch without external libraries like PyTorch or TensorFlow.
The neural network (forward prop, backprop, gradient descent) is implemented by hand in NumPy.
Draw a digit in the browser and a locally running python backend predicts it.
